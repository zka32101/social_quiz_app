org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m
android.overridePathCheck=true
org.gradle.daemon=false
android.useAndroidX=true
# Disable Kotlin incremental compilation: causes "different roots" errors
# when building from a path containing Japanese characters (H:\マイドライブ)
kotlin.incremental=false
kotlin.incremental.android=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
